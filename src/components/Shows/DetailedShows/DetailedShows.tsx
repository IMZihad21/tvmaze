import Show from "../../../interfaces/Show";

interface iProps {
    show: Show
}

const DetailedShows = ({ show }: iProps) => {
    return (
        <div>

        </div>
    );
};

export default DetailedShows;